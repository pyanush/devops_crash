name: mykola_shtefanovych_19021973
on:
  push:
    branches:
    - mykola_shtefanovych_19021973/docker_projekt
jobs:
 compose-projekt:
   runs-on: self-hosted
   steps:      
    - name: docker-compose1
      run: 
        cd /home/testuser/mykola/my_CI/devops_crash
         git pull
         git checkout mykola_shtefanovych_19021973
         cd docker_projekt
         docker-compose up  -d --force-recreate
