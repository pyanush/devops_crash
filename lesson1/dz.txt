#Create the crash user expected to be on machines ?
sudo echo ‘crash ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/crash
sudo chmod 0440 /etc/sudoers.d/crash
sudo chown -R crash:crash /home/crash/.ssh/
#Disable SSH access for root 
sudo sed -i 's/#PermitRootLogin yes/PermitRootLogin no/g' \ /etc/ssh/sshd_config
sudo systemctl restart sshd